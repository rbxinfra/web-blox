/* Shim for Rating */

import Rating from '@mui/material/Rating';

export {
  ratingClasses,
  type RatingProps as TRatingProps,
  type RatingClasses as TRatingClasses,
  type RatingClassKey as TRatingClassKey
} from '@mui/material/Rating';

export default Rating;