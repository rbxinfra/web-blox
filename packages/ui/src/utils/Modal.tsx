/* Shim for Modal */

import Modal from '@mui/material/Modal';

export {
  modalClasses,
  type ModalProps as TModalProps,
  type ModalClasses as TModalClasses,
  type ModalClassKey as TModalClassKey
} from '@mui/material/Modal';

export default Modal;